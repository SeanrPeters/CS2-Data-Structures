
#include <cstdlib>
#include <iostream>
#include <fstream>
#include "string.hpp"
#include "utilities.hpp"


int main(int inp, char *inp[])
 {

    if (in != 3 && inp != 2)
       output_usage_and_exit(inp[0]);

      std::ifstream in(inp[1]);

    if (!in)
    {
        std::cerr << "Error: Could not open file! " << inp[1] << std::endl;
        exit(2);
     }
 }

